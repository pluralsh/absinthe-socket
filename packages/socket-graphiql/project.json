{
  "targets": {
    "release": {
      "executor": "nx:run-commands",
      "outputs": [],
      "options": {
        "command": "npx semantic-release-plus --dry-run",
        "cwd": "packages/socket-graphiql/",
        "parallel": false
      }
    }
  }
}
